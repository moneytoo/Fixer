<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!--<ListPreference
        android:defaultValue="https://www.google.com/search?q=%s"
        android:entries="@array/pref_engine_titles"
        android:entryValues="@array/pref_engine_values"
        android:key="engine"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_engine" />

    <EditTextPreference
        android:defaultValue="@string/pref_default_engine_custom"
        android:inputType="textUri"
        android:dependency="engine"
        android:key="engine_custom"
        android:title="@string/pref_title_engine_custom" />

    <Preference
        android:key="engine_test"
        android:title="@string/pref_title_engine_test" />-->

    <SwitchPreference
        android:key="pref_no_safe_volume_warning"
        android:title="No safe volume warning"
        android:defaultValue="true"/>

    <SwitchPreference
        android:key="pref_keyboard_switching"
        android:title="Hacker's Keyboard only in Termux"
        android:defaultValue="true"/>

    <Preference
        android:key="pref_sip"
        android:title="SIP settings" />

</PreferenceScreen>
